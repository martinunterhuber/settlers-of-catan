<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.settlersofcatan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SettlersOfCatan">
      
        <activity android:name=".GameEndActivity"
            android:parentActivityName=".MainActivity"/>
      
        <activity
            android:name=".TradeActivity"
            android:label="@string/title_activity_trade"
            android:theme="@style/Theme.SettlersOfCatan.Trade"/>

        <activity android:name=".GameActivity"
            android:theme="@style/GameTheme"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
      
        <activity
            android:name=".server_client.WaitForHostActivity"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>